using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stats : MonoBehaviour
{
    // Player Stats
    public static int playerlvl = 1;

    public static float playerHealth = 20f;
    public static float playerMaxHealth = 20f;

    public static float playerMana = 10f;
    public static float playerMaxMana = 10f;

    public static int exp = 0;
    public static int maxexp = 10;

    public static float playerDamage = 6f;
    public static float playerAtkSpeed = 1f;

    // Movement related variables
    public static float playerWalkSpeed = 6f;
    public static float playerRunSpeed = 8.5f;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
